server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        # 配置Nacos地址
        #        server-addr: localhost:8848
        server-addr: 172.25.115.122:8848

management:
  endpoints:
    web:
      exposure:
        include: '*'